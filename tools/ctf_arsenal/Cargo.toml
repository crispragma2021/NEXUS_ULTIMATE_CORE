[package]
name = "ctf-arsenal"
version = "1.0.0"
edition = "2024"
description = "CTF Mobile Capture Toolkit — Rust Pure, Zero External Deps"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
